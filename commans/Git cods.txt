-----
A = Added
M = Modified 
U = untrack  
-----

Git cods :


for create git repo:
$ git init

for delet git repo:
$ rm -rf ~/.git

next checking files:
$ git status

next for checking all files:
$ ls -la

next add file to git hub:
$ git add {file name}

next add all files:
$ git add .


q (exit karne me kis kis me use hota hai):
$ git log
$ git show
$ git diff

next commit files they add in git:
$ git commit -m "First Commit"
( har bar git add . fir git commit karna hota hai)


for checking git commit ID:
$ git log (all details)
$ git log --oneline (for only check git commit ID and message('-m))

if you want go to old commit code:
$ git checkout ('git Id)

if you want to go master(new one or current):
$ git switch master (means header)

if you want to check branch name:
$ git branch 

if you wnat to creater a branch:
$ git branch loging 




